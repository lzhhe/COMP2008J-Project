/*
package test.general;

import card.Card;
import card.CardKind;
import general.*;

import card.actionCard.ActionCard;
import card.actionCard.PassGo;
import card.actionCard.steal.StealDeal;
import card.moneyCard.MoneyCard;
import card.propertyCard.PropertyCard;
import card.propertyCard.wildCard.WildCard;

import java.time.LocalDate;
import java.util.ArrayList;
import java.util.Objects;
import java.util.Scanner;
import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import static general.Game.*;


*/
/**
 * test for general.Round
 *
 * @author tianj
 *//*

public class RoundTest {

    @Before
    public void before() throws Exception {
    }

    @After
    public void after() throws Exception {
    }

    */
/**
     * Method: Round()
     *//*

    @Test
    public void testRound() throws Exception {
        Player player = new Player("john", LocalDate.of(2001, 9, 10));
        Round round = new Round(player);
        Assert.assertNotNull(round);
    }
    */
/**
     * Method: oneStep()
     *//*

    @Test
    public void testOneRound() throws Exception {
        Player player = new Player("john", LocalDate.of(2002, 9, 10));
        Round round = new Round(player);
        Assert.assertNotNull(round);
        int status = round.oneStep();
        Assert.assertTrue(status == -1 || status == 0);
    }
}*/
